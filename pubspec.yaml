name: api_request_helper_flutter
description: Api Request Helper Flutter is a repository that handles http calls such as GET, POST, PUT, DELETE
version: 1.6.2+22
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  eventsource: ^0.4.0
  exceptions_flutter:
    git:
      url: https://github.com/ChargePlus/exceptions_flutter.git
  flutter:
    sdk: flutter
  hashids2: ^2.0.0
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.1.0
